#include<iostream>
using namespace std;

main(){
	int length,width,height;
	cout << "Calculate the cuboid volume:\n";
	cin >> length;
	cin >> width;
	cin >> height;
	cout << "The cuboid volume is: " << length*width*height;
}
